# Lecture 10

## 纹理（剩余部分）

**纹理可以理解为数据，可以进行查询**

![image-20260528101325014](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528101325014.png)

### Environment Map

例：右图依据左图渲染，把环境光应用到纹理上

![image-20260528101548382](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528101548382.png)

存储环境光的Spherical Environment Map（将环境光记录在球面）

![image-20260528105811922](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528105811922.png)

- 但是展开球面后会出现扭曲的Problem

- 解决办法：认为记录的球有个包围盒

![image-20260528110034171](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528110034171.png)

​	将球面的内容打到立方体的六个面

**纹理还可以用作凹凸贴图** Bump Mapping

核心观点是：**纹理不仅仅可以用来存储颜色（Albedo），还可以用来存储几何信息（如高度或法线），从而在不增加网格三角形数量的前提下，通过影响光照计算来“伪造（Fake）”出复杂的表面细节。**

![image-20260528110531697](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528110531697.png)



## Geometry

### 一、 隐式几何 (Implicit Geometry)

![image-20260528192908170](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528192908170.png)

**定义**：不直接给出点的坐标，而是通过一个**数学关系式/方程**来间接定义表面。所有满足方程 

```
f(x,y,z)=0
```

 的点共同构成了这个几何表面。

#### 1. 常见形式

- **代数曲面 (Algebraic Surface)**：例如一个完美的球体方程：`x2+y2+z2−r2=0x2+y2+z2−r2=0`。
- **有向距离场 (Signed Distance Function, SDF)**：空间中任意点到几何表面的最短距离。若点在物体内部，距离为负；在外部为正；在表面上为 `00`。
- **水平集 (Level Sets)**、**体素 (Voxels)**。

#### 2. 优缺点

- **优点**：**判断内外极快**：只需将点代入方程，看结果是大于、小于还是等于 `00`。**拓扑结构变化极其容易**：非常适合模拟流体（如水滴融合、分裂）和进行实体布尔运算（CSG，如相交、相减）。**无限分辨率**：数学公式定义的表面在任何距离下都是绝对光滑的。
- **缺点**：很难直接找到表面上具体某一个点的坐标。

#### 3. CUDA / GPU 实现方法：**光线步进法 (Ray Marching)**

由于隐式几何没有三角面片，GPU 的传统光栅化管线无法直接渲染它。在 CUDA 中，我们通常使用 **Ray Marching（光线步进/球体追踪）** 来渲染 SDF：

- **算法逻辑**：从相机发射光线，每次沿着光线方向前进一步，步长由该点的 SDF 值决定。当 SDF 值接近 `00` 时，说明找到了交点。
- **CUDA 优势**：每个像素发射的光线互不干扰，非常适合并行化。现代技术如 **NeRF（神经辐射场）** 的体渲染就是用 CUDA 实现的高效隐式光线采样。

------



### 二、 显式几何 (Explicit Geometry)

**定义**：直接给出所有点的具体坐标，或者通过参数方程（如二维参数空间到三维空间的映射 

```
f(u,v)=(x,y,z)
```

）将表面上的所有点直接罗列出来。



#### 1. 常见形式

- **多边形网格 (Polygon Mesh)**：最常用的三角形网格（vertices 和 indices 数组）。
- **点云 (Point Cloud)**：三维空间中离散点的集合。
- **细分曲面 (Subdivision)**、**NURBS**。

#### 2. 优缺点

- **优点**：**极易渲染**：现代 GPU 硬件（如 Rasterization 管线）就是专门为处理三角形网格而设计的。**直观**：容易进行纹理贴图（UV 映射）和形变（如骨骼动画）。
- **缺点**：**内外判断困难**：判断一个点是否在复杂的兔子模型内部，计算开销很大。**拓扑改变困难**：如果要将一个网格撕裂成两个，需要复杂的几何重构计算。

#### 3. CUDA / GPU 实现方法：**BVH 树与光线-三角形求交**

显式几何（如三角形网格）是光线追踪的主力。在 CUDA 中：

- **算法逻辑**：为了避免光线与场景中的每一个三角形进行求交计算（开销呈 `O(N)O(N)`），我们会在 CUDA 中构建 **BVH（层次包围盒）** 树。光线首先与包围盒求交，只有相交时才继续深入子节点，最后与三角形求交（开销降为 `O(log⁡N)O(logN)`）。
- **CUDA 硬件加速**：NVIDIA RTX 显卡中的 **RT Core（光追核心）** 在硬件层面加速了这一过程，而 OptiX（NVIDIA 的光追引擎）和 CUDA 深度结合，使得开发者可以高效处理数亿个显式三角形。

![image-20260528193140099](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528193140099.png)

### 为什么说“隐式几何（SDF）”是 CSG 的绝配？

在不同的几何表示下，实现 CSG 的难度天差地别：

1. **显式几何（多边形网格）下：极其困难**若要对两个由三角形构成的网格做差集，需要精确计算成百上千个三角形的相交线，对三角形进行剪裁、缝合、重新三角化。这涉及极复杂的数值稳定性问题，极易出现缝隙或崩溃。
2. **隐式几何（SDF）下：极其简单**在有向距离场中，CSG 运算退化成了最基础的**数学比较大小**。





补充：**有向距离场（Signed Distance Field，简称 SDF）**，也常被称为**有向距离函数（Signed Distance Function）**，是计算机图形学、物理模拟和三维视觉中一种非常强大且优雅的**隐式几何表示方法**。简单来说，SDF 是一个空间标量场。对于空间中的任意一个点，SDF 都会告诉我们：**“这个点距离最近的几何表面有多远，以及它是在物体内部还是外部。”**

![image-20260528194513554](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528194513554.png)

![image-20260528193902501](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528193902501.png)

Example：

![image-20260528195601036](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528195601036.png)

### 第一部分（第一排）：直接混合颜色/不透明度（失败的做法）

这是最直观但**错误**的做法：直接把图像 A 和图像 B 的像素值相加取平均数（线性插值）。

- **状态 A**：左边 `1/31/3` 是实心的（黑色斜线，不透明度为 1）。
- **状态 B**：左边 `2/32/3` 是实心的（不透明度为 1）。
- **直接混合 blend(A, B)**：最左边 `1/3：A 是 `1.0，B 是 `1.0，平均值是 `1.0`（依然是实心）。中间的 `1/3`：A 是空心的（`0.0`），B 是实心的（`1.0`），平均值是 **`0.5`（半透明！）**。最右边 `1/3`：平均值是 `0.0`。

#### 结果（不好的地方）：

边界并没有真正“移动”起来。中间那段没有变成实心，而是产生了一个**半透明的“鬼影”（Ghosting/Fade-in）**。这在视觉上看起来像是“淡入淡出”，而不是一堵墙平滑地滑过去。

### 第二部分（第二排）：混合 SDF 距离场（成功的做法）

如果我们不混合颜色，而是**混合它们到边界的距离（SDF）**，结果会发生神奇的变化。

图中底部的三个框中：0表示边界，+表示外部，-表示内部。

我们来做个简单的数学计算：

1. 在 **SDF(A)** 中，边界 `0 `在 `1/3` 处。

2. 在 **SDF(B)** 中，边界 `0 `在 `2/3` 处。

3. 现在我们把这两个距离场相加取平均，看看**正中间** 1/2 **处**的值会变成多少：

   - 在 **SDF(A)** 中，`1/2` 处在边界右侧，属于外部，距离值是正数。
   - 在 **SDF(B)** 中，`1/2` 处在边界左侧，属于内部，距离值是负数。
   - **混合后 blend(SDF(A), SDF(B)) 在** 1/2 **处的值** `0`

   结果：

- 边界左边依然是完美的实心（负值区）。
- 边界右边依然是完美的空心（正值区）。
- **没有任何半透明的“鬼影”！**

![image-20260528200258420](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528200258420.png)

### 分形：类似于递归

![image-20260528200913830](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260528200913830.png)

**分形**是指具有**自相似性（Self-similarity）**的几何结构。

- **自相似性**：这意味着如果你把这个图形的某一部分放大，你会发现它和整体长得几乎一模一样。
- **无限细节（Detail at all scales）**：传统的 3D 模型（如显式几何的三角形）放大到一定程度就会看到斑驳的三角形边缘（分辨率极限）。但分形不同，**无论你放大多少倍，它都有无穷无尽的新细节呈现出来**，永无止境。
