# Lecture 8 and Lecture 9



## 着色的三种方式

1. `Flat` shading:对三角形着色
2. `Gouraud` shading：对顶点着色
3. `Phong` shading：对像素点着色

![image-20260526153932910](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526153932910.png)

- 问题：逐顶点着色的顶点的法线怎么确定？

![image-20260526154323766](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526154323766.png)

解：求这个点相邻面的法线的`加权`平均



## Graphics Pipeline

![image-20260526155217785](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526155217785.png)

![image-20260526155649455](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526155649455.png)



## 纹理映射

- 纹理映射的完整实现依赖于以下三个核心步骤：

1. **定义 UV 坐标**：
   三维模型上的每个顶点除了有空间坐标 `(x,y,z)(x,y,z)` 外，还拥有一个二维的纹理坐标 `(u,v)(u,v)`，范围通常在 `[0,1][0,1]`。这决定了该顶点对应二维贴图上的哪一个点。

   

2. **光栅化与插值**：
   当 3D 三角形投影到 2D 屏幕上进行光栅化时，GPU 使用**重心坐标插值**计算出三角形内部每个屏幕像素对应的 `(u,v)(u,v)` 坐标。

   

3. **纹理采样（Sampling）**：
   GPU 根据插值得到的 `(u,v)(u,v)` 坐标去纹理图片中查找颜色。如果坐标不是整数，就会使用**双线性插值（Bilinear）**等过滤算法计算出一个平滑的颜色，最终渲染到屏幕上。

   

- 重心坐标：为了做三角形内的插值

![image-20260526165828495](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526165828495.png)

- 如何求重心坐标？

`奔驰定理`:![image-20260526170155085](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526170155085.png)

`或者通过坐标计算`：

![image-20260526170543647](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526170543647.png)

- 应用重心坐标：

![image-20260526170645063](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526170645063.png)

- 问题：投影后重心坐标可能会发生变化

解：在投影之前计算重心坐标，比如说想插值三维空间中的坐标，应该去三维空间中的坐标计算重心坐标做插值再投影。



## Applying Textures

- 纹理的放大问题：

  这个问题是怎么引起的？

  它的根本原因在于：**屏幕分辨率（像素 Pixel）与纹理图分辨率（纹素 Texel）的不匹配**。

  具体场景是**当照相机（Camera）离物体非常近的时候**：

  1. **尺寸不匹配**：假设墙壁上贴了一张分辨率很低的木纹贴图（比如 `256×256256×256` 纹素）。当玩家走得极近时，这块墙壁在你的 `1080p1080p` 屏幕上可能要占满 `1000×10001000×1000` 个像素。
  2. **一个纹素覆盖多个像素**：这意味着，贴图上**仅仅 1 个格子（纹素）**，在屏幕上需要被拉伸并填充到**大约 16 个像素**的区域。
  3. **最近邻采样的缺陷（Nearest Neighbor）**：
     在没有双线性插值前，GPU 采用最简单的“最近邻采样”——屏幕像素映射到纹理坐标后，离哪个纹素中心最近，就直接复制哪个纹素的颜色。结果就是：屏幕上连续的 16 个像素都采到了同一个纹素的颜色。在视觉上，你就会看到原本应该平滑的木纹变成了**一个一个巨大的彩色方块（马赛克）**，也就是俗称的“满屏狗牙”。

![image-20260526182101273](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526182101273.png)

双线性插值：主要用来解决**纹理放大（Texture Magnification）导致的“马赛克”（方块效应/锯齿）问题**。

![image-20260526182757804](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526182757804.png)

- 一个像素覆盖了纹理的很大一部分区域，用像素中心代表一大块纹理区域的情况。出现欠采样问题。

![image-20260526184431459](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526184431459.png)

![image-20260526184059880](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526184059880.png)

​	解决方法：

 1.超采样（思考一个像素内纹理的**频率**很高，希望重构信息需要更高频的采样，但是增加成本，不是希望的）

 2.考虑不采样，**范围查询**（Mipmap）

在图形学中，对纹理的查询有两种：

1. **点查询（Point Query）**：*问题*：“我想知道纹理坐标在 `(u,v)(u,v)` 这个精确点上的颜色是什么？”*解决办法*：**双线性插值**（Bilinear Interpolation）。
2. **范围查询（Range Query）**：*问题*：“屏幕上的这一个像素，投影到纹理上覆盖了一个大小为 `D×DD×D` 的**区域（Footprint）**。我想知道这个区域内所有纹素的**平均颜色**是什么？”*难点*：如果每次都去数这个区域里有多少个纹理像素，然后加起来求平均（称为直接重采样），计算复杂度是 `O(面积)O(面积)`。远处一个像素可能覆盖数万个纹素，每帧算几百万次，显卡会直接瘫痪。**目标**：我们需要一种**时间复杂度为** `**`O(1)O(1)`**`（常数时间，即不管区域多大，都要瞬间算出）的范围查询方法。

**Mipmap 就是为了实现** 

```
O(1)O(1)
```

 **范围查询而诞生的技术。**

### 什么是 Mipmap（多级渐远纹理）？

Mipmap 的核心思想是：**空间换时间，提前做好预计算。**

#### 图像金字塔（Pyramid）

“Mip” 来自拉丁语 *multum in parvo*，意为“小空间里的大世界”。
GPU 会在渲染开始前，把原始纹理（Level 0）不断等比例缩小（每次分辨率减半），直到缩到 

```
1×11×1
```

 像素（最后一层）。



codeTEXT



```
Level 0:  [256 x 256]  (原始图)
Level 1:  [128 x 128]  (长宽各减半)
Level 2:  [ 64 x  64]  
...
Level 8:  [  1 x   1]  (金字塔顶端)
```

#### 内存开销

生成这些缩小的图会占用额外内存吗？是的，但是代价极小：

```
额外内存=14+116+164+⋯=13额外内存=41+161+641+⋯=31
```



即：**只需要增加 `33.3%33.3%`（三分之一）的额外内存**，就能换取极高的渲染性能。这在图形学中是极其划算的买卖。

![image-20260526185331772](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526185331772.png)

**如何精确计算一个屏幕像素应该去采样 Mipmap 的第几层**

![image-20260526190207558](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526190207558.png)

在屏幕空间（左图）中，像素是规则排列的：

- 从当前像素往右移动 `11` 个像素（即 `dx=1dx=1`），它在右侧纹理空间（`u,vu,v` 轴坐标系）中对应的坐标变化量就是 `(dudx,dvdx)(dxdu,dxdv)`。
- 同理，往上移动 `11` 个像素（即 `dy=1dy=1`），在纹理空间中的变化量就是 `(dudy,dvdy)(dydu,dydv)`。

要选择max?

**安全策略**：为了防止走样（抗锯齿），我们采取**保守（偏大）\**的估计，选择水平拉伸和垂直拉伸中\**更长的那一个**作为正方形的边长 `LL`。



####  为什么 `D=log⁡2L` ？

- 在 Mipmap 的第 `DD` 层，原本在第 0 层（原图）大小为 `2D×2D` 的区域，已经被预计算并压缩成了单个像素。

- 既然我们算出了当前像素在原图上覆盖了 `L×L 的区域，为了让这个区域正好缩成 `1` 个像素，我们需要满足：
  `2D=L`

- 两边取以 2 为底的对数，就得到了最终公式：
  `D=log⁡2L`

  

如果计算得到的D不为整数，可以**在两个相邻的整数层之间，再做一次线性插值。**称为**三线性插值**。

![image-20260526191954309](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526191954309.png)

- Mipmap局限：

### Mipmap 解决不了的“斜视”问题

- **各向同性（Isotropic）**：Mipmap 的每一步缩放，都是**长宽同时减半**。也就是说，预计算的图像永远是**正方形**的（比如 `128×128128×128`）。
- **斜视场景**：
  想象你站在一条无限延伸的马路上（或者侧着头贴近一面墙）。**垂直方向（纵深）**：因为透视严重，网格被极度压缩（高频）。**水平方向**：压缩并不严重（低频）。此时，屏幕一个像素在纹理空间中覆盖的区域，不再是正方形，而是一个**极其扁长的矩形（甚至长条椭圆形）**。

#### Mipmap 的尴尬：

因为 Mipmap 只有正方形。为了不产生走样，它只能用一个**包裹住整个长条的巨大正方形**去代替它（如下图所示）。

- 
- **结果**：在水平方向上，它把大量本该清晰的像素强行平均化了，导致**画面在斜视时变得极其模糊（Over-blurring）**。

![image-20260526192518526](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526192518526.png)

解决办法：**各项异性过滤**

### 各向异性过滤（AF）是如何解决的？

“各向异性”的意思是：**在不同方向上，缩放比例可以不同。**

#### 1核心原理：引入矩形预计算

除了生成正方形的 Mipmap，各向异性过滤还提前生成了各种**长宽比不同的长方形贴图**。

```
标准 Mipmap 只沿着对角线缩放：
[256x256] -> [128x128] -> [64x64]

各向异性过滤（Ripmap）会把长方形也算出来：
[256x128], [256x64], [256x32] ... (水平压缩)
[128x256], [64x256], [32x256] ... (垂直压缩)
```



![image-20260526192615760](C:\Users\huashuo\AppData\Roaming\Typora\typora-user-images\image-20260526192615760.png)

